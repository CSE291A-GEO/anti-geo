┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: DATA EXTRACTION (run_geo.py:105-108)                   │
├─────────────────────────────────────────────────────────────────┤
│ dataset = load_dataset("GEO-Optim/geo-bench", 'test')          │
│ entry = dataset['test'][0]                                      │
│   - query: "mention the names of any 3 famous folklore..."     │
│   - sugg_idx: 3  ← YOUR target source                          │
│   - sources: [5 pre-defined sources from dataset]              │
│                                                                  │
│ ❌ Problem: dataset sources are NEVER USED!                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: SEARCH (run_geo.py:63 → utils.py:242)                  │
├─────────────────────────────────────────────────────────────────┤
│ improve(query, idx=3, summaries=None) ← No summaries passed!   │
│   ↓                                                              │
│ get_answer(query, summaries=None)                              │
│   ↓                                                              │
│ if summaries is None:  ← TRUE!                                 │
│     search_results = search_handler(query)  ← LIVE WEB SEARCH  │
│                                                                  │
│ Result: 4 sources from Google Custom Search API:               │
│   [0] embibe.com                                                │
│   [1] huggingface.co/datasets/GEO-Optim/geo-bench              │
│   [2] newindianexpress.com                                      │
│   [3] karnataka.com/profile/traditional-games/ ← YOUR source   │
│                                                                  │
│ 🚨 These are DIFFERENT from dataset sources!                    │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: BASELINE ANSWER GENERATION (run_geo.py:63-69)          │
├─────────────────────────────────────────────────────────────────┤
│ generate_answer(query, summaries=[4 web sources])              │
│   ↓ Ollama (qwen2.5:3b) generates 5 different answers          │
│                                                                  │
│ answers = [answer1, answer2, answer3, answer4, answer5]        │
│ sources = [source0, source1, source2, source3]                 │
│ summaries = [summary0, summary1, summary2, summary3]           │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: BASELINE EVALUATION (run_geo.py:75-78)                 │
├─────────────────────────────────────────────────────────────────┤
│ For each of 5 answers:                                          │
│   - Extract citations: [1], [2], [3], etc.                     │
│   - Count citations per source                                  │
│                                                                  │
│ init_scores = [0.2, 0.2, 0.2, 0.2, 0.0]                        │
│                               ↑                                  │
│                    YOUR source (idx=3) = 20%                    │
│                                                                  │
│ Note: Only 4 sources, so source[4] gets 0%                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: GEO OPTIMIZATION LOOP (run_geo.py:82-95)               │
├─────────────────────────────────────────────────────────────────┤
│ For each GEO method (10 methods):                              │
│                                                                  │
│   summaries_copy = [                                            │
│     summaries[0],                 # Keep original              │
│     summaries[1],                 # Keep original              │
│     summaries[2],                 # Keep original              │
│     GEO_METHOD(summaries[3]),     # ← OPTIMIZE THIS!           │
│   ]                                                              │
│                                                                  │
│   Example: more_quotes_mine(summaries[3])                      │
│     - Takes: "Traditional games in Karnataka..."               │
│     - Returns: "Traditional games... As Dr. X stated..."       │
│     - Calls Ollama to add authoritative quotes                 │
│                                                                  │
│   answers = get_answer(query, summaries=summaries_copy)        │
│     ↓ Now summaries IS provided, so NO web search              │
│     ↓ Ollama generates new answers using modified summaries    │
│                                                                  │
│   Calculate new citation scores                                 │
│   improvement = final_scores - init_scores                      │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: RESULTS (run_geo.py:96-101)                            │
├─────────────────────────────────────────────────────────────────┤
│ improvements = [                                                │
│   [Δ0, Δ1, Δ2, Δ3, Δ4],  ← identity                           │
│   [Δ0, Δ1, Δ2, Δ3, Δ4],  ← fluent_gpt                         │
│   [Δ0, Δ1, Δ2, Δ3, Δ4],  ← unique_words_gpt                   │
│   ...                                                            │
│   [Δ0, Δ1, Δ2, Δ3, Δ4],  ← seo_optimize_mine2                 │
│ ]                                                                │
│                                                                  │
│ Column 3 (YOUR source improvements):                            │
│   improvements[:, 3] = [0.0, 0.0, 0.08, 0.0, 0.15, ...]        │
│                                                                  │
│ Success boolean: improvements[:, 3] > 0                         │
│   = [False, False, True, False, True, ...]                     │
└─────────────────────────────────────────────────────────────────┘